import 'package:flutter/widgets.dart' show BorderRadius, Radius;

/* Borders */
const BorderRadius borderRadiusTiny = BorderRadius.all(Radius.circular(5.0));
const BorderRadius borderRadiusSmall = BorderRadius.all(Radius.circular(10.0));
const BorderRadius borderRadiusRegular = BorderRadius.all(
  Radius.circular(18.0),
);
const BorderRadius borderRadiusMedium = BorderRadius.all(Radius.circular(25.0));
const BorderRadius borderRadiusLarge = BorderRadius.all(Radius.circular(50.0));
